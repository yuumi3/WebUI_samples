# WebUI sample

## Build

#### React app.

```sh
$ cd vite_react
$ npm install
$ npm run build
$ cp -r dist/* ..
$ cd ..
```

#### deno

```sh
$ deno run --allow-all --unstable react.ts
```

#### C

```sh
$ make
$ ./main
```